using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class JGUICinema : MonoBehaviour {

	private static JTableCmdParam.CinemaParam m_param;
	// Use this for initialization
	static void SetParam (JTableCmdParam.CinemaParam param) {
		m_param = param;
	}

	void Awake()
	{
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
